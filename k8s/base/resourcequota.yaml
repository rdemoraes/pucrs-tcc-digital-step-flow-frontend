apiVersion: v1
kind: ResourceQuota
metadata:
  name: digital-step-flow-quota
  namespace: digital-step-flow
spec:
  hard:
    requests.cpu: "4"
    requests.memory: 8Gi
    limits.cpu: "8"
    limits.memory: 16Gi
    persistentvolumeclaims: "10"
    pods: "20"

